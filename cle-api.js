/* ------------------------------------------------------------------
   Clé API
   Collez la clé entre les guillemets, enregistrez le fichier, c'est tout.
   Elle se crée sur console.anthropic.com (section « API keys »).
   Sans clé ici, l'application la demandera au premier lancement.
------------------------------------------------------------------- */

window.CLE_API = "";
